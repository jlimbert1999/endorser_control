export interface chargeResponse {
  _id: string;
  name: string;
  denomination: string;
  register: number;
  id_unit: string;
  id_level: string;
  level_class: number;
  category: string;
  classification: string;
  contract: string;
  state: boolean;
}
